PORT=4000

# BOT Chain testnet
CHAIN_ID=
RPC_URL=
EXPLORER_URL=
CONTRACT_ADDRESS=

# Data sources
ETHERSCAN_API_KEY=

# LLM
LLM_PROVIDER=openai
LLM_API_KEY=

# Telegram narration (optional — silently no-ops if unset). One bot token for the
# host — it joins every guest's private pairing chat and replies into whichever
# chat_id the incoming request specifies (per-guest, sent by the guest itself).
HOST_BOT_TOKEN=
