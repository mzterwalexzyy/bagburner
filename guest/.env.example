GUEST_ID=0
GUEST_COUNT=3
GUEST_PRIVATE_KEY=

# This guest's own bot + its private pairing chat with the host (host bot is also a member)
GUEST_BOT_TOKEN=
TELEGRAM_CHAT_ID=

CHAIN_ID=
RPC_URL=
CONTRACT_ADDRESS=

HOST_URL=http://localhost:4000
LOOP_INTERVAL_MS=300000

# LLM (used to compose this guest's chat messages — same key as host is fine)
LLM_PROVIDER=openai
LLM_BASE_URL=
LLM_MODEL=
LLM_API_KEY=
